<%= form_with(model: message_template, local: true) do |form| %>
  <% if @errors %>
    <div id="error_explanation">
      <h2><%= pluralize(@errors.count, "error") %> prohibited this message_template from being saved:</h2>

      <ul>
        <% @errors.each do |message| %>
          <li><%= MessageTemplate.human_attribute_name(message.path.first) %> <%= message.text %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
